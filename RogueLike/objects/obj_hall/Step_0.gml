/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor
	
var dist = point_distance(x, y, obj_player.x, obj_player.y);
	
if dist < 60 and !alarm[0] > 0 and obj_controle.objetivo
{
	alarm[0] = room_speed * 1;
}








